package com.example.back_PT.repository;

import com.example.back_PT.entity.Gestora;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GestoraRepository extends JpaRepository<Gestora, Long> {
}